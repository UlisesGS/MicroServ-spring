spring.application.name=msvc-procesos
server.port=${PORT:0}
eureka.instance.instance-id=${spring.application.name}:${random.value}
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
spring.data.mongodb.uri=mongodb://localhost:27017/db_proceso




# Configuración del acceso a MongoDB
#spring.data.mongodb.username=myuser  # Nombre de usuario de MongoDB (si se usa autenticación)
#spring.data.mongodb.password=mypassword  # Contraseña de MongoDB (si se usa autenticación)


